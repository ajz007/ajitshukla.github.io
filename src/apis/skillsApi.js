import {backendSkills, frontEndSkills, devopsSkills} from '../mock/mockSkills'

export function getBackendSkills() {
    return backendSkills; // can be updated to database or rest api call later
}

export function getFrontEndSkills() {
    return frontEndSkills; // can be updated to database or rest api call later
}

export function getDevopsSkills() {
    return devopsSkills; // can be updated to database or rest api call later
}